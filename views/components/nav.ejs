<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img width="auto" height="50px" class="d-inline-block align-top" src="/public/logo.png" alt="Logo AlbsigBay">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar"
                aria-controls="navbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbar">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link h1" href="/">AlbsigBay</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link h1 <% if(page.toLowerCase() === "home"){ %> active<% } %>" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link h1 <% if(page.toLowerCase() === "about"){ %> active<% } %>"
                       href="/about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link h1 <% if(page.toLowerCase() === "faq"){ %> active<% } %>" href="/faq">FAQ</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <% if (typeof user !== 'undefined') { %>
                    <% if (!user) { %>
                        <li class="nav-item">
                            <a class="nav-link h1 <% if(page.toLowerCase() === "login"){ %> active<% } %>" href="/login">Login</a>
                        </li>
                    <% }else{ %>
                        <% if (user.isAdmin) {%>
                            <li class="nav-item">
                                <a class="nav-link h1 <% if(page.toLowerCase() === "admin"){ %> active<% } %>" href="/admin">Admin</a>
                            </li>
                        <% } %>
                        <li class="nav-item">
                            <a class="nav-link h1 <% if(page.toLowerCase() === "profile"){ %> active<% } %>" href="/profile/<%= user.id%>">Profil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link h1" href="/logout">Logout</a>
                        </li>
                    <% } %>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link h1 <% if(page.toLowerCase() === "login"){ %> active<% } %>" href="/login">Login</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
